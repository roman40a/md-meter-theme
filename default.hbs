<!DOCTYPE html>
<html lang="{{@site.lang}}">
<head>

    {{!-- Document Settings --}}
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />

    {{!-- Base Meta --}}
    <title>{{meta_title}}</title>
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    {{!-- Styles'n'Scripts --}}
    <link rel="stylesheet" type="text/css" href="{{asset "built/global.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/header.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/site-footer.css"}}" />

    {{!-- home --}}
    <link rel="stylesheet" type="text/css" href="{{asset "built/home/top-logo.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/home/options-list-mobile.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/home/options-list-ipad-desktop.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/home/reviews.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/home/newest-posts.css"}}" />

    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet" />

    {{!-- This tag outputs SEO meta+structured data and other important settings --}}
    {{ghost_head}}

</head>
<body class="{{body_class}}">

    <div class="site-wrapper">

        {{> "site-header/site-header"}}
        <div class="body-wrapper">
            {{{body}}}
        </div>

        {{> "site-footer/site-footer"}}
    </div>

    {{!-- Ghost outputs important scripts and data with this tag - it should always be the very last thing before the closing body tag --}}
    {{ghost_foot}}

</body>
</html>
